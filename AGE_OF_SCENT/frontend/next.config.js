/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: false
    },
    reactStrictMode: true,
    allowedDevOrigins: ["10.221.146.179"]
};

module.exports = nextConfig;
